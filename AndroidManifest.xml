<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="%(app.package)s"
    android:versionCode="%(app.version.revision)s"
    android:versionName="%(app.version)s">

    <uses-sdk android:minSdkVersion="%(android.minapi)s"
              android:targetSdkVersion="%(android.api)s"/>

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.VIBRATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="%(app.title)s"
        android:theme="@style/AppTheme">

        <!-- Kivy/Python Activity -->
        <activity
            android:name="org.kivy.android.PythonActivity"
            android:configChanges="orientation|screenSize|smallestScreenSize|keyboard|keyboardHidden|screenLayout|locale|fontScale"
            android:exported="true"
            android:label="%(app.title)s"
            android:launchMode="singleTask"
            android:screenOrientation="%(android.orientation)s"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- NekoClick 无障碍服务 -->
        <service
            android:name="org.nekoclick.NekoClickService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService"/>
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config"/>
        </service>

    </application>
</manifest>
